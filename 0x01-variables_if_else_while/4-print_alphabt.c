#include <stdio.h>
/**
 * main - Prints alphabet in lowercase.
 *
 * Return: 0
 */
int main(void)
{
	char ch;

	while (ch = 'a' ; ch <= 'z' ; ch++)
	{
	putchar (ch);
	}
	putchar ('\n');
	return (0);
}i
